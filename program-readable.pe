vars:

	num x;
	num y;
	num z;


subprograms:

	num sum(num first, num second) {
		vars:
			num result;

		process:
			result = first + second;
	} 


process:

	x = 5 + 4;
	y = x;
	z = y + y + 1;
