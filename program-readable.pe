vars:

	num x;
	num y;
	num z;
	num mult;
	num problema;


subprograms:

	num sum(num first, num second) {
		vars:
			num result;

		process:
			result = first + second;
	} 


process:

	x = 5 + 4;
	y = x;
	z = y + y + 1;
	c = 2;
	mult = 5 * 2;
	mult = mult - 2;
	mult = mult ^ 2;
	problema = x ^ 2;

