vars:

num x;
num y;


subprograms:

num sum(num first, num second) {
	vars:
	num result;

	process:
	result = first + second;

	return result;
} 


process:

x = 5;
y = 2;
sum(x, y);